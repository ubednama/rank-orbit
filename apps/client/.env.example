# =============================================================================
# client (Next.js) — required env vars
# =============================================================================
# Only NEXT_PUBLIC_* vars are exposed to the browser. Anything else is
# server-only (this app currently has no server-only env needs).

# ---- API gateway URL (browser → gateway) ----
NEXT_PUBLIC_GATEWAY_URL=http://localhost:3333
